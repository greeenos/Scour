<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Scour.Views.FileSearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Scour.Models">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="File Search" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource PageSubtitle}"
                       Text="Search a folder tree by name. Useful for tracking down stray files an app left behind." />
        </StackPanel>

        <ProgressBar Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource LoadingBar}" IsIndeterminate="True"
                     Visibility="{x:Bind Vm.IsSearching, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

        <Grid Grid.Row="2" Margin="0,12" ColumnSpacing="8" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Search in folder..." Text="{x:Bind Vm.Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="Browse..." Click="OnBrowse" />
            <TextBox Grid.Column="2" PlaceholderText="Name contains..." Text="{x:Bind Vm.Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="3" Content="Search" Style="{ThemeResource AccentButtonStyle}" Command="{x:Bind Vm.SearchCommand}" />
            <Button Grid.Column="4" Content="Cancel" Command="{x:Bind Vm.CancelCommand}" />
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="6">
            <ListView ItemsSource="{x:Bind Vm.Results, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemContainerTransitions>
                    <TransitionCollection>
                        <AddDeleteThemeTransition />
                    </TransitionCollection>
                </ListView.ItemContainerTransitions>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:FileItem">
                        <Grid Padding="6,6" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" MinWidth="32" />
                            <FontIcon Grid.Column="1" Glyph="{x:Bind Kind, Converter={StaticResource KindToGlyph}}"
                                      Foreground="{StaticResource PearAccentBrush}" />
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}" />
                                <TextBlock Text="{x:Bind Path}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <TextBlock Text="{x:Bind Vm.StatusText, Mode=OneWay}" VerticalAlignment="Center" Style="{StaticResource PageSubtitle}" />
            <Button HorizontalAlignment="Right" Content="Remove selected" Click="OnRemove" />
        </Grid>
    </Grid>
</Page>
