<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Scour.Views.OrphansPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Scour.Models">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Orphaned Files" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource PageSubtitle}"
                       Text="Folders in AppData / ProgramData that don't match any installed application — likely left behind by software you've already uninstalled." />
        </StackPanel>

        <ProgressBar Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource LoadingBar}" IsIndeterminate="True"
                     Visibility="{x:Bind Vm.IsScanning, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,12">
            <Button Content="Scan" Style="{ThemeResource AccentButtonStyle}" Command="{x:Bind Vm.ScanCommand}" />
            <Button Content="Select all" Command="{x:Bind Vm.SelectAllCommand}" />
            <Button Content="Deselect all" Command="{x:Bind Vm.DeselectAllCommand}" />
        </StackPanel>

        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="6">
            <ListView ItemsSource="{x:Bind Vm.Orphans, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemContainerTransitions>
                    <TransitionCollection>
                        <AddDeleteThemeTransition />
                    </TransitionCollection>
                </ListView.ItemContainerTransitions>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:FileItem">
                        <Grid Padding="6,8" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" MinWidth="32" />
                            <FontIcon Grid.Column="1" Glyph="&#xE8B7;" Foreground="{StaticResource PearAccentBrush}" />
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Name}" />
                                <TextBlock Text="{x:Bind Path}" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <TextBlock Grid.Column="3" Text="{x:Bind SizeDisplay}" VerticalAlignment="Center"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <TextBlock Text="{x:Bind Vm.StatusText, Mode=OneWay}" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" Style="{StaticResource PageSubtitle}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <TextBlock VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="Selected: " /><Run Text="{x:Bind Vm.SelectedSizeDisplay, Mode=OneWay}" />
                </TextBlock>
                <Button Content="Remove selected" Style="{ThemeResource AccentButtonStyle}" Click="OnRemoveClick"
                        IsEnabled="{x:Bind Vm.IsRemoving, Mode=OneWay, Converter={StaticResource InverseBool}}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
